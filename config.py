MAX_CAPACITY_MEM_CACHE = 50  # set 1 < capacity < MAX (measured in number of messages) to save context in memory(RAM)
CACHE_TYPE = 'mem'  # if stay blank by default cache setting in memory, options = ['mem', 'db']
